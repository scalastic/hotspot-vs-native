version: "3"

services:

  rng:
    image: rng-hotspot:0.0.1-SNAPSHOT
    build:
      context: ./rng
      dockerfile: ./Dockerfile.hotspot

  hasher:
    image: hasher-hotspot:0.0.1-SNAPSHOT
    build:
      context: ./hasher
      dockerfile: ./Dockerfile.hotspot

  webui:
    build: webui
    ports:
    - "80:80"
    volumes:
    - "./webui/files/:/files/"

  redis:
    image: redis:6.2.1

  worker:
    build: worker
